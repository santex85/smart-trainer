# fail2ban filter for Caddy auth (login/register 401 or 429)
# Copy to /etc/fail2ban/filter.d/caddy-auth.conf on the server

[Definition]
failregex = ^<HOST> .* "(POST /api/v1/auth/login|POST /api/v1/auth/register)" (401|429)
ignoreregex =
