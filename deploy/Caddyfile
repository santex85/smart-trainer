# Production: set DOMAIN in .env (e.g. trainer.example.com).
# Caddy will obtain a Let's Encrypt certificate and proxy to frontend.
{$DOMAIN} {
  reverse_proxy frontend:80 {
    transport http {
      read_timeout 300s
      write_timeout 300s
    }
  }
}
